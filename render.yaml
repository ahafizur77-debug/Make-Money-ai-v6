services:
  - type: web
    name: moneymind-ai-v6
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: APP_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 20
